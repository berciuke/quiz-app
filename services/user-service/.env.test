# Test environment configuration
NODE_ENV="test"
PORT=3002

# Test database configuration  
DATABASE_URL="postgresql://quiz_user:quiz_password@localhost:5432/quiz_app_test_db"

# JWT configuration for tests
JWT_SECRET="test-super-secret-jwt-key-for-tests-only"
JWT_EXPIRATION="1h"

# Rate limiting (disabled for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# CORS (allow all for tests)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"

# Service URLs for tests
QUIZ_SERVICE_URL="http://localhost:3003" 